# Optional. Raises Hugging Face Hub rate limits when downloading the
# sentence-transformers and Qwen models on first run. The app works
# fine without it - you'll just see a rate-limit warning in the logs.
HF_TOKEN=
